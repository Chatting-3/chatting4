CREATE TABLE MEMBER (
	USER_ID	NVARCHAR2(20)	NOT NULL,
    USER_PWD	NVARCHAR2(20)	NULL,
	USER_NAME	NVARCHAR2(20)	NULL,
    gender NVARCHAR2(20) NULL,
    age NUMBER NULL,
	NICKNAME	NVARCHAR2(20)	NULL,
	ENROLL_DATE	DATE	NULL,
	DELETE_YN	NVARCHAR2(10) DEFAULT 'N',
	DELETE_DATE	NVARCHAR2(20)	NULL
);

ALTER TABLE MEMBER ADD CONSTRAINT PK_MEMBER PRIMARY KEY (
	USER_ID
);

CREATE TABLE CHATROOM (
    CR_NO NUMBER NOT NULL,
    CR_NAME NVARCHAR2(20) NULL,
    CR_ID NVARCHAR2(20) NULL,
    CR_CURRENT NUMBER,
    CR_CREATE_DATE DATE DEFAULT SYSDATE
    );
    ALTER TABLE CHATROOM ADD CONSTRAINT PK_CHATROOM PRIMARY KEY (
	CR_NO
);

drop table chatroom;

CREATE SEQUENCE SEQ_CID
START WITH 1;

drop sequence seq_cid;


select * 
from chatroom
order by CR_NO DESC;


